on:
  push:
    branches:
      - main
    tags-ignore:
      - '**'

name: on-commit-to-main

jobs:
  Verify:
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - name: Print event data
        env:
          EVENT: ${{ toJSON( github.event ) }}
        run: echo "$EVENT"
